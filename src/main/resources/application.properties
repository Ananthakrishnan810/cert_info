server.port=27080
spring.application.name=gmail-messaging-service

# SQLite Database Configuration
spring.datasource.url=jdbc:sqlite:cert_info.db
spring.datasource.driver-class-name=org.sqlite.JDBC

# Gmail SMTP Configuration (Port 587 STARTTLS)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${GMAIL_USERNAME:ethanhuntim10@gmail.com}
spring.mail.password=${GMAIL_APP_PASSWORD:imozwjgnnhnwxjrh}

# Spring Mail SMTP Properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2 TLSv1.3
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# Custom Application Properties
gmail.from-name=Certification renewal reminder
gmail.default-sender=${GMAIL_USERNAME:ethanhuntim10@gmail.com}
# Set dev-mode to true to simulate delivery locally if network ISP blocks outbound SMTP (port 587/465)
gmail.dev-mode=${GMAIL_DEV_MODE:false}

# Logging Configuration
